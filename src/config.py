SSUP_config = dict(
    num_init = 3,
    num_iter = 10,
    num_sim = 4,
    eps = 0.2,
    eps_decay_rate = 0.95,
    lr =1000,
    max_attempts = 100,
    max_simulations = 1000,
    attempt_threshold = 0.7,
)

OURS_config = dict(
    eps = 0.2,
    max_attempts = 100,
    max_simulations = 1000,
    num_trials = 100,
    attempt_threshold = 0.7,
)

